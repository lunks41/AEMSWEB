  <!-- LaunchService Modal -->
<div class="modal fade" id="addLaunchServiceModal" tabindex="-1" aria-labelledby="addLaunchServiceModalLabel" aria-hidden="true" data-bs-backdrop="static">
    <div class="modal-dialog modal-dialog-scrollable modal-xl modal-dialog-centered">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="addLaunchServiceModalLabel">Launch Services</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                <form id="launchServiceForm">
                    <input type="hidden" id="launchService_jobOrderId" />
                    <input type="hidden" id="launchServiceId" />
                    <input type="hidden" id="companyId" />
                    <input type="hidden" id="launchService_jobOrderId" />
                    <input type="hidden" id="launchService_jobOrderNo" />
                    <input type="hidden" id="launchService_debitNoteId" />
                    <input type="hidden" id="launchService_debitNoteNo" />
                    <div class="row">
                        <div class="col-md-3">
                            <label class="form-label">Launch Date <span class="text-danger">*</span></label>
                            <input type="date" class="form-control" id="launchService_Date" required>
                        </div>
                        <div class="col-md-3">

                            <label for="launchServices_cmb_launch" class="form-label">Launch <span class="text-danger">*</span></label>
                            <select id="launchServices_cmb_launch" style="width: 100%"></select>
                        </div>
                        <div class="col-md-3">

                            <label for="launchServices_cmb_charge" class="form-label">Charge <span class="text-danger">*</span></label>
                            <select id="launchServices_cmb_charge" style="width: 100%"></select>
                        </div>
                        <div class="col-md-3">

                            <label for="launchServices_cmb_gl" class="form-label">Gl Account <span class="text-danger">*</span></label>
                            <select id="launchServices_cmb_gl" style="width: 100%"></select>
                        </div>
                    </div>

                    <div class="row mt-1">
                        <div class="col-md-3">

                            <label class="form-label">Distance To Vessel</label>
                            <input type="number" class="form-control" id="distanceFromJettyToVessel" step="0.0001">
                        </div>
                        <div class="col-md-3">

                            <label for="launchServices_cmb_uom" class="form-label">UOM  <span class="text-danger">*</span></label>
                            <select id="launchServices_cmb_uom" style="width: 100%"></select>
                        </div>

                        <div class="col-md-3">

                            <label class="form-label">Agent Tally</label>
                            <input type="text" class="form-control" id="agentTally">
                        </div>
                        <div class="col-md-3">

                            <label class="form-label">Boat Opt Tally</label>
                            <input type="text" class="form-control" id="boatopTally">
                        </div>
                    </div>

                    <div class="row mt-1">
                        <div class="col-md-3">
                            <label class="form-label">Lunch Hire Time (1)</label>
                            <input id="loadingTime" placeholder="dd/MM/yy HH:mm">
                        </div>
                        <div class="col-md-3">

                            <label class="form-label">Left Jetty (2)</label>
                            <input id="leftJetty" placeholder="dd/MM/yy HH:mm">
                        </div>
                        <div class="col-md-3">

                            <label class="form-label">Alongside Vessel (3)</label>
                            <input id="alongsideVessel" placeholder="dd/MM/yy HH:mm">
                        </div>
                        <div class="col-md-3">

                            <label class="form-label">Departed From Vessel (4)</label>
                            <input id="departedFromVessel" placeholder="dd/MM/yy HH:mm">
                        </div>
                    </div>

                    <div class="row mt-1">
                        <div class="col-md-3">
                            <label class="form-label">Arrived At Jetty (5)</label>
                            <input id="arrivedAtJetty">
                        </div>
                        <div class="col-md-4 d-flex align-items-center justify-content-center">
                            <b>Waiting Time At Loading Pt (2-1) : <span id="time2minus1"></span></b>
                        </div>
                        <div class="col-md-4 d-flex align-items-center justify-content-center">
                            <b>Waiting Time Along Side (4-3) : <span id="time4minus3"></span></b>
                        </div>
                    </div>

                    <div class="row mt-1">
                        <div class="col-md-3">
                            <label class="form-label">Weight Delivered</label>
                            <div class="input-group">
                                <input type="number" class="form-control" id="weightOfCargoDelivered" step="0.0001" value="0.00">
                                <span class="input-group-text">Tons/Pallets</span>
                            </div>
                        </div>
                        <div class="col-md-3">
                            <label class="form-label">Weight Landed</label>
                            <div class="input-group">
                                <input type="number" class="form-control" id="weightOfCargoLanded" step="0.0001" value="0.00">
                                <span class="input-group-text">Tons/Pallets</span>
                            </div>
                        </div>
                        <div class="col-md-3">
                            <label class="form-label">Boat Operator</label>
                            <input type="text" class="form-control" id="boatoperator">
                        </div>

                        <div class="col-md-3">
                            <label class="form-label">Credit Inv No</label>
                            <input type="text" class="form-control" id="creditorsInvNo">
                        </div>
                    </div>

                    <div class="row mt-1">
                        <div class="col-md-3">
                            <label class="form-label">Annexure</label>
                            <input type="text" class="form-control" id="annexure">
                        </div>
                        <div class="col-md-3">
                            <label for="launchServices_cmb_port" class="form-label">Launch From (Port)</label>
                            <select id="launchServices_cmb_port" style="width: 100%"></select>
                        </div>
                        <div class="col-md-3">
                            <label for="launchServices_cmb_status" class="form-label">Status  <span class="text-danger">*</span></label>
                            <select id="launchServices_cmb_status" style="width: 100%"></select>
                        </div>
                    </div>
                    <div class="col-md-12">
                        <label class="form-label">Remark</label>
                        <textarea class="form-control" id="launchServices_remarks" rows="2"></textarea>
                    </div>

                    <!-- Audit Fields -->
                    <div class="accordion mt-3" id="auditAccordion">
                        <div class="accordion-item">
                            <h2 class="accordion-header">
                                <button class="accordion-button collapsed" type="button" data-bs-toggle="collapse" data-bs-target="#auditCollapse">
                                    Audit Details
                                </button>
                            </h2>
                            <div id="auditCollapse" class="accordion-collapse collapse">
                                <div class="accordion-body">
                                    <div class="row">
                                        <div class="col-md-6">
                                            <label class="form-label">Created By</label>
                                            <p id="launchServices_createBy" class="form-control-plaintext"></p>
                                        </div>
                                        <div class="col-md-6">
                                            <label class="form-label">Created Date</label>
                                            <p id="launchServices_createDate" class="form-control-plaintext"></p>
                                        </div>
                                        <div class="col-md-6">
                                            <label class="form-label">Modified By</label>
                                            <p id="launchServices_editBy" class="form-control-plaintext"></p>
                                        </div>
                                        <div class="col-md-6">
                                            <label class="form-label">Modified Date</label>
                                            <p id="launchServices_editDate" class="form-control-plaintext"></p>
                                        </div>
                                        <div class="col-md-6">
                                            <label class="form-label">Edit Version</label>
                                            <p id="launchServices_editVersion" class="form-control-plaintext"></p>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </form>

                <div class="col-12">
                    <div class="alert alert-info mt-3">
                        <p>Note:</p>
                        <ol>
                            <li>Launch Hire Time must and should be less than or equals to Left Jetty Time.</li>
                            <li>Left Jetty Time must and should be less than or equals to Along Side Vessel.</li>
                            <li>Along Side Vessel must and should be less than or equals to Departed From Vessel.</li>
                            <li>Departed From Vessel must and should be less than or equals to Back To Base.</li>
                        </ol>
                    </div>
                </div>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" id="btnLaunchServiceClear" onclick="clearLaunchServiceForm()">Clear</button>
                <button type="button" class="btn btn-primary" id="btnLaunchServiceSave" onclick="saveLaunchService()">Save</button>
            </div>
        </div>
    </div>
</div>

<script>

    function refreshLaunchServiceDropdown() {

        /*initializeDateTimePicker("#arrivedAtJetty", new Date(2025, 3, 17, 14, 30), { format: "dd/MM/yy HH:mm", parseFormats: ["dd/MM/yy HH:mm:ss", "dd/MM/yy"] });*/

       // initializeMaskedDateTime("#loadingTime");

        initializeDateTimePicker("#departedFromVessel,#alongsideVessel,#leftJetty,#loadingTime,#arrivedAtJetty");

        const launchUrl = '@Url.Action("GetBargeLookup", "Lookup", new { area = "" })?companyId=' + companyId;
        bindComboBox(launchUrl, "launchServices_cmb_launch", "bargeName", "bargeId");

        const chartOfAccountUrl = '@Url.Action("GetChartOfAccountLookup", "Lookup", new { area = "" })?companyId=' + companyId;
        const glcolumnsProperties = [
            { field: 'glCode', title: 'Code', width: 100 },
            { field: 'glName', title: 'Name', width: 200 }
        ];
        const glfilterFields = ['glCode', 'glName'];
        bindMultiColumnComboBox(chartOfAccountUrl, "launchServices_cmb_gl", "glName", "glId", glcolumnsProperties, glfilterFields);

        const chargeUrl = `@Url.Action("GetChargeLookup", "Lookup", new { area = "" })?companyId=${companyId}&taskId=2`;
        bindComboBox(chargeUrl, "launchServices_cmb_charge", "chargeName", "chargeId");

        const uomUrl = '@Url.Action("GetUomLookup", "Lookup", new { area = "" })?companyId=' + companyId;
        bindComboBox(uomUrl, "launchServices_cmb_uom", "uomName", "uomId");

        const statusUrl = `@Url.Action("GetOrderTypeLookup", "Lookup", new { area = "" })?companyId=${companyId}&categoryId=4`;
        bindComboBox(statusUrl, "launchServices_cmb_status", "orderTypeName", "orderTypeId");

        const portUrl = '@Url.Action("GetPortLookup", "Lookup", new { area = "" })?companyId=' + companyId;
        bindComboBox(portUrl, "launchServices_cmb_port", "portName", "portId");
    }

    function SelectedDropdown(DrpdwnName) {
    }

    function OnSelectDropdown(dataItem, DrpdwnName) {
    }

    function calculateTimeDifferences() {
        // Get input elements
        const loadingTime = document.getElementById('loadingTime');
        const leftJetty = document.getElementById('leftJetty');
        const alongsideVessel = document.getElementById('alongsideVessel');
        const departedFromVessel = document.getElementById('departedFromVessel');

        // Get values and convert to dates
        const time1 = new Date(loadingTime.value);
        const time2 = new Date(leftJetty.value);
        const time3 = new Date(alongsideVessel.value);
        const time4 = new Date(departedFromVessel.value);

        // Calculate differences in milliseconds
        const diff2minus1 = time2 - time1;
        const diff4minus3 = time4 - time3;

        // Format time function
        function formatTime(duration) {
            if (duration < 0) return "Invalid time sequence";

            const hours = Math.floor(duration / 3600000);
            const minutes = Math.floor((duration % 3600000) / 60000);

            return `${hours} hours ${minutes} minutes`;
        }

        // Display results
        document.getElementById('time2minus1').textContent = formatTime(diff2minus1);
        document.getElementById('time4minus3').textContent = formatTime(diff4minus3);
    }

    function clearLaunchServiceForm() {

        $('#launchServicesId').val('');
        $('#launchServices_jobOrderId').val('');
        $('#launchServices_jobOrderNo').val('');
        $('#launchServices_debitNoteId').val('');
        $('#launchServices_debitNoteNo').val('');

        $('#launchServices_cmb_gl').data('kendoMultiColumnComboBox').value('');

        $('#launchServices_cmb_charge').data('kendoComboBox').value('');
        $('#launchServices_cmb_uom').data('kendoComboBox').value('');
        $('#launchServices_cmb_status').data('kendoComboBox').value('');

        $('#launchServices_quantity').val('');
        $('#launchServices_deliverDate').val('');
        $('#isActive').prop('checked', true);
        $('#launchServices_remarks').val('');

        $('#launchServices_createBy').text('');
        $('#launchServices_createDate').text('');
        $('#launchServices_editBy').text('');
        $('#launchServices_editDate').text('');
        $('#launchServices_editVersion').text('');

        $('#btnLaunchServiceClear').show();
    }

    function setDefaultDropdownValues() {
        // Set default GL Account
        $('#launchServices_cmb_gl').data('kendoMultiColumnComboBox').value(488);

        // Set default to first charge
        setTimeout(() => {
            const chargeCombo = $('#launchServices_cmb_charge').data('kendoComboBox');
            if (chargeCombo.dataSource.view().length > 0) {
                chargeCombo.select(0);
            }
        }, 100);

        // Set default to first UOM
        setTimeout(() => {
            const uomCombo = $('#launchServices_cmb_uom').data('kendoComboBox');
            if (uomCombo.dataSource.view().length > 0) {
                uomCombo.select(0);
            }
        }, 100);

        // Set default status to "Confirm"
        setTimeout(() => {
            const statusCombo = $('#launchServices_cmb_status').data('kendoComboBox');
            const confirmItem = statusCombo.dataSource.data().find(item =>
                item.orderTypeName.toLowerCase() === "confirmed"
            );
            if (confirmItem) {
                statusCombo.value(confirmItem.orderTypeId);
            }
        }, 100);
    }

    function openLaunchServiceModal(jobOrderId, jobOrderNo, launchServicesId, mode) {

        const $modal = $('#addLaunchServiceModal');
        const parsedJobOrderId = parseInt(jobOrderId, 10);
        const parsedLaunchServiceId = parseInt(launchServicesId, 10);
        $('#launchServices_jobOrderId').val(jobOrderId);
        $('#launchServices_jobOrderNo').val(jobOrderNo);

        const isValidId = id => !isNaN(id) && id !== null && id !== "" && id > 0;

        // Edit Mode: Fetch existing data and populate the form
        if (isValidId(parsedJobOrderId) && isValidId(parsedLaunchServiceId)) {

            $modal.find('.modal-content').append('<div class="modal-overlay"><div class="spinner"></div></div>');

            $.get('@Url.Action("GetLaunchServiceById", "Job", new { area = "Project" })', {
                jobOrderId: parsedJobOrderId,
                launchServicesId: parsedLaunchServiceId,
                companyId: $('#companyId').val()
            })
                .done(function (response) {
                    if (response.success) {
                        refreshLaunchServiceDropdown();
                        clearLaunchServiceForm();
                        populateLaunchServiceModalFields(response.data);
                        $('#addLaunchServiceModal').modal('show');
                    }
                })
                .always(() => $modal.find('.modal-overlay').remove());
        } else {
            // Add Mode: Clear form and set default values
            refreshLaunchServiceDropdown();
            clearLaunchServiceForm();
            setDefaultDropdownValues();

            $('#addLaunchServiceModal').modal('show');
        }
    }

    function populateLaunchServiceModalFields(data) {

        // Set hidden fields
        $('#companyId').val(data.companyId);
        $('#launchServicesId').val(data.launchServicesId);
        $('#launchServices_jobOrderId').val(data.jobOrderId);
        $('#launchServices_jobOrderNo').val(data.jobOrderNo);
        $('#launchServices_debitNoteId').val(data.debitNoteId);
        $('#launchServices_debitNoteNo').val(data.debitNoteNo);

        const chargeCombo = $('#launchServices_cmb_charge').data('kendoComboBox');
        chargeCombo.value(data.chargeId > 0 ? data.chargeId : '');
        chargeCombo.one('dataBound', function () {
            chargeCombo.value(data.chargeId > 0 ? data.chargeId : '');
        });

        const glCombo = $('#launchServices_cmb_gl').data('kendoMultiColumnComboBox');
        glCombo.value(data.glId > 0 ? data.glId : '');
        glCombo.one('dataBound', function () {
            glCombo.value(data.glId > 0 ? data.glId : '');
        });

        const uomCombo = $('#launchServices_cmb_uom').data('kendoComboBox');
        uomCombo.value(data.uomId > 0 ? data.uomId : '');
        uomCombo.one('dataBound', function () {
            uomCombo.value(data.uomId > 0 ? data.uomId : '');
        });

        const statusCombo = $('#launchServices_cmb_status').data('kendoComboBox');
        statusCombo.value(data.statusId > 0 ? data.statusId : '');
        statusCombo.one('dataBound', function () {
            statusCombo.value(data.statusId > 0 ? data.statusId : '');
        });

        // Set other fields
        $('#launchServices_quantity').val(data.quantity);
        const formattedDate = new Date(data.deliverDate).toLocaleDateString('en-CA');
        $('#launchServices_deliverDate').val(formattedDate);
        $('#isActive').prop('checked', data.isActive);
        $('#launchServices_remarks').val(data.remarks);

        // Set audit fields
        $('#launchServices_createBy').text(data.createBy || "N/A");
        $('#launchServices_createDate').text(data.createDate ? new Date(data.createDate).toLocaleString() : "N/A");
        $('#launchServices_editBy').text(data.editBy || "N/A");
        $('#launchServices_editDate').text(data.editDate ? new Date(data.editDate).toLocaleString() : "N/A");
        $('#launchServices_editVersion').text(data.editVersion || "0");

        // Hide clear button in edit mode
        $('#btnLaunchServiceClear').hide();

        // Enable/disable fields based on debitNoteId
        if (data.debitNoteId > 0) {
            chargeCombo.enable(false);
            uomCombo.enable(false);
            statusCombo.enable(false);
            supplierCombo.enable(false);
            glCombo.enable(false);
            $('#launchServices_quantity, #launchServices_deliverDate,  #launchServices_remarks').prop('readonly', true);
            $('#isActive').prop('disabled', false);
            $('#btnLaunchServiceSave').prop('disabled', false);
        } else {
            chargeCombo.enable(true);
            uomCombo.enable(true);
            statusCombo.enable(true);
            supplierCombo.enable(true);
            glCombo.enable(true);
            $('#launchServices_quantity, #launchServices_deliverDate,  #launchServices_remarks').prop('readonly', false);
            $('#isActive').prop('disabled', false);
            $('#btnLaunchServiceSave').prop('disabled', false);
        }
    }

    function deleteLaunchService(jobOrderId, launchServicesId) {
        showDeleteModal("Are you sure you want to delete this record?", function () {
            $.ajax({
                url: '@Url.Action("DeleteLaunchService", "Job", new { area = "Project" })',
                type: "DELETE",
                data: { jobOrderId, launchServicesId, companyId: companyId },
                success: function (response) {
                    if (response.success) {
                        loadLaunchServiceGrid();
                        $('#confirmationModal').modal('hide');
                    }
                }
            });
        });
    }

    function saveLaunchService() {

        let launchServicesData = {
            LaunchServiceId: parseInt($('#launchServicesId').val()) || 0,
            JobOrderId: parseInt($('#launchServices_jobOrderId').val()) || 0,
            JobOrderNo: $('#launchServices_jobOrderNo').val() || '',
            ChargeId: parseInt($('#launchServices_cmb_charge').data('kendoComboBox').value()) || 0,
            GLId: parseInt($('#launchServices_cmb_gl').data('kendoMultiColumnComboBox').value()) || 0,
            Quantity: parseFloat($('#launchServices_quantity').val()) || 0,
            UomId: parseInt($('#launchServices_cmb_uom').data('kendoComboBox').value()) || 0,
            DeliverDate: $('#launchServices_deliverDate').val() || '',
            StatusId: parseInt($('#launchServices_cmb_status').data('kendoComboBox').value()) || 0,
            Remarks: $('#launchServices_remarks').val(),
            IsActive: 1,
            DebitNoteId: parseInt($('#launchServices_debitNoteId').val()) || 0,
            DebitNoteNo: $('#launchServices_debitNoteNo').val() || '',
        };

        console.log("LaunchService Data : " + launchServicesData);

        $.ajax({
            url: '@Url.Action("SaveLaunchService", "Job", new { area = "Project" })',
            type: "POST",
            data: JSON.stringify({ launchServices: launchServicesData, companyId }),
            contentType: "application/json",
            success: function (response) {
                if (response.success) {
                    initializeLaunchServiceGrid();
                    clearLaunchServiceForm();
                    $('#addLaunchServiceModal').modal('hide');
                } else {
                    alert(response.message || "Failed to save port expense.");
                }
            },
            error: function (xhr) {
                alert("Error: " + (xhr.responseJSON?.message || "An unexpected error occurred"));
            }
        });
    }
</script>